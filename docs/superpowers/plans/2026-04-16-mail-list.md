# Mail List Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Land end-to-end mail-list contact import parity in Flutter using the existing `MailListPage` UI and the real `user/maillist` backend contract.

**Architecture:** Keep `MailListPage` focused on presentation and callback orchestration, move mail-list models and runtime logic into dedicated files, and isolate native contact access behind a small gateway so tests stay deterministic. Use a stateless runtime flow for now: read contacts, normalize, upload, fetch matches, merge, display.

**Tech Stack:** Flutter, Dio, `flutter_contacts`, `permission_handler` (existing app dependency), widget tests, API contract tests

---

### Task 1: Lock the API Contract With Tests

**Files:**
- Modify: `lib/core/config/api_config.dart`
- Modify: `lib/service/api/user_api.dart`
- Test: `test/service/api/user_api_test.dart`

- [ ] Add a failing test that verifies upload uses `POST /v1/user/maillist` with `[{name, zone, phone}]`.
- [ ] Run: `flutter test test/service/api/user_api_test.dart`
- [ ] Add a failing test that verifies fetch uses `GET /v1/user/maillist` and parses `uid`, `vercode`, and `is_friend`.
- [ ] Run: `flutter test test/service/api/user_api_test.dart`
- [ ] Add the `ApiConfig.userMailList` constant and typed `UserApi` methods to make the tests pass.
- [ ] Re-run: `flutter test test/service/api/user_api_test.dart`

### Task 2: Build a Testable Mail-List Runtime

**Files:**
- Create: `lib/data/models/mail_list_contact.dart`
- Create: `lib/service/mail_list/mail_list_service.dart`
- Test: `test/service/mail_list/mail_list_service_test.dart`

- [ ] Add failing service tests for Android-compatible normalization (`name`, `phone`), phone dedupe, and matched-before-unmatched merge behavior.
- [ ] Run: `flutter test test/service/mail_list/mail_list_service_test.dart`
- [ ] Implement the minimal mail-list models and service with injected contact source + API dependency.
- [ ] Re-run: `flutter test test/service/mail_list/mail_list_service_test.dart`

### Task 3: Wire the Page Default Loader

**Files:**
- Modify: `lib/wukong_uikit/search/mail_list_page.dart`
- Test: `test/wukong_uikit/search/mail_list_page_parity_test.dart`

- [ ] Add a failing widget test that proves the page loads through its default path when `onLoadContacts` is omitted.
- [ ] Add a failing widget test for loading/error state behavior.
- [ ] Run: `flutter test test/wukong_uikit/search/mail_list_page_parity_test.dart`
- [ ] Update the page to use the new shared model and default mail-list service while preserving existing injected callback seams.
- [ ] Re-run: `flutter test test/wukong_uikit/search/mail_list_page_parity_test.dart`

### Task 4: Add Native Dependency and Permissions

**Files:**
- Modify: `pubspec.yaml`
- Modify: `android/app/src/main/AndroidManifest.xml`
- Modify: `ios/Runner/Info.plist`

- [ ] Add the contacts plugin dependency.
- [ ] Add Android contacts permission.
- [ ] Add iOS contacts usage description.
- [ ] Run: `flutter pub get`

### Task 5: Verify the Slice and Refresh Gap Tracking

**Files:**
- Modify: `C:\Users\COLORFUL\Desktop\WuKongIM\WuKongIM_Migration_Gap_Analysis_Report.md`

- [ ] Run focused verification:
  - `flutter test test/service/api/user_api_test.dart test/service/mail_list/mail_list_service_test.dart test/wukong_uikit/search/mail_list_page_parity_test.dart`
- [ ] Run a broader regression touchpoint:
  - `flutter test test/service/api/group_api_test.dart test/service/api/message_api_test.dart test/modules/chat/chat_page_scene_flow_test.dart`
- [ ] Run: `flutter build windows --debug`
- [ ] Update the migration gap report so `mail list` no longer remains listed as missing and note already-stale items such as pinned messages.
