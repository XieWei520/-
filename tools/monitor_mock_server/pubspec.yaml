name: monitor_mock_server
description: Local mock Monitor API server for Feishu Agent MVP smoke tests.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.11.1

dev_dependencies:
  test: ^1.26.0
