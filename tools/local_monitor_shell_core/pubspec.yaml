name: local_monitor_shell_core
description: Shared local monitor shell HTTP, SSE, snapshot, and persistence core.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.11.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
